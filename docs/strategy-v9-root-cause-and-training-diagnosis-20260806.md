# 策略版本 9：现金空仓根因与训练区间诊断

> 日期：2026-08-06
> 策略版本：数据库 `StrategyVersion.id = 9`
> 状态：`operational_validated`
> 训练诊断代码基线：`abc9501`
> 结论：现金空仓及 2021～2022 训练折新增暴露的银行业覆盖、金融因子族和涨跌停证据问题均已修复；三个带 purge/embargo 的训练走步折共 13 期全部产生真实目标与成交，优化器全部为 `optimal`。版本 9 后续正式历史验证已经完成，运行链路通过但投资有效性失败，详见[正式历史验证报告](strategy-v9-formal-historical-validation-20260806.md)。

## 一、版本 8 为什么会“回测完成但始终持有现金”

版本 8 的历史验证虽然生成了净值曲线，但所有参数组合都没有目标持仓、成交、换手和交易费用。净值变化只来自现金利息，因此不能视为策略有效性证据。版本 9 从选股输入到组合求解逐层复现，确认不是单一阈值问题，而是四个相互串联的实现缺陷。

### 1. 不可用财务主源错误覆盖了可用回退源

- Tushare 三表主源在部分历史报告期缺少资产负债表关键勾稽字段，快照被正确标记为 `formal_factor_usable = false`。
- 仓储合并层却只按“主源存在”判断同期去重，无条件删除 Eastmoney/StockToday 的可用回退财务指标。
- 因子层随后正确跳过不可用主源，导致质量因子最终没有任何可用 ROE。
- 在 2017-02-28 的原始漏斗中，603 只通过股票池过滤的股票里只有约 29 只满足完整因子权重覆盖。

修复后，仅通过质量门禁的 Tushare 主源可以覆盖同期回退数据；不合格主源仍保留作审计记录，但不会遮蔽可用回退 ROE。相同日期的因子合格股票增加到 319～330 只。

### 2. 全局 Top N 与行业主动偏离硬约束的候选集相互冲突

- 原实现先按全市场综合分截取 Top N，再要求组合相对基准的单行业主动偏离不超过 3%。
- 全局 Top N 可能完全遗漏基准中的重要行业。
- 正式优化器又把非 Top N 股票的允许权重设为零，因此缺失行业没有任何可调证券，问题在数学上不可行。

版本 9 在正式优化模式下先计算历史信号日的自由流通市值行业权重，为基准权重大于 3% 的行业预留足以承载“基准权重减 3%”的候选席位，再按综合分补足 Top N。研究性非正式模式仍保持原来的全局 Top N 语义。

### 3. 启发式组合在正式优化前被边界误判清空

- 2017-02-28 的启发式初始组合暴露为 `size=-0.200282`、`beta=+0.15000000000000002`。
- 原代码在正式凸优化器执行前直接清空超限组合，Beta 还包含典型的浮点边界误判。
- 优化器从未获得继续求解的机会。

版本 9 增加明确的数值验收容差；正式模式下，启发式权重只作为初始候选，不再替代凸优化器作最终可行性裁决。Size、Beta、流动性分别使用 0.20、0.15、0.20 的独立硬上限，求解后按最终权重重新计算并验收。

### 4. 优化器保留的旧持仓在求解后被组合层删除

- 后续调仓时，优化器可以保留部分当前持仓以满足换手、行业和风格约束。
- 原代码在求解结束后只保留“本期新候选”代码，直接删除优化器合法保留的旧持仓。
- 被二次删改的权重不再是优化器的可行解，2017-03-31 因此出现“求解状态 optimal，但最终风格超限并清仓”。

版本 9 为优化器增加逐证券目标权重上限：本期候选按风险容量允许持有，候选外旧持仓的目标权重固定为零，同时保留足够交易容量生成退出订单。求解结果和最终目标现在使用同一个可行域，不再在求解后静默改变持仓集合。

### 5. 银行业专用监管数据只有契约、没有历史数据

- 2021-08-31 的动态股票池、行情、财务、估值和普通因子均正常，但基准中的银行业权重为 13.5343%。
- 旧数据链路没有下载净息差、不良贷款率、拨备覆盖率和资本充足率，银行业 30 只股票中没有任何一只满足专用模型，候选行业容量为 0。
- 在行业主动偏离不得超过 3% 的硬约束下，银行业至少需要 10.5343% 权重，组合因此在数学上不可行。

修复后，通过东方财富公开财务接口下载正式区间历史上 39 家银行的完整主要指标，以真实公告日作为 `available_at`，登记 2,494 条不可变 PIT 规范记录并保留原始 Parquet、源文件哈希和逐字段血缘。银行指标披露频率不一致时按字段执行 `last observation carried forward`，但只允许使用信号日已公告的记录。

### 6. 金融专用模型错误清空三个共同价格因子族

- 补齐监管数据后，平安银行等银行的质量、价值专用字段已经完整，`model_eligible=true`。
- 但 `_family_policy` 对金融证券的五个因子族都无条件查询专用特征字典；该字典只定义质量和价值字段。
- 动量、趋势、低波三个族因此得到空字段集，银行可用权重被错误计算为 53.8462%，低于 75% 入选门槛，银行业仍被全体剔除。

修复后，金融专用契约只替换质量和价值族；所有证券共同使用的动量、趋势、低波价格因子继续参与评分。该缺陷已增加独立回归测试。

## 二、同时修复的验证与可观测性问题

- 正式走步验证新增真实策略活动硬门禁：任一训练折若没有非空目标、没有成交或平均换手为零，立即失败，并附带首个调仓漏斗和告警，不再耗时运行验证集及一次性留出集。
- 每期调仓新增 `selection_funnel`，记录信号股票数、股票池通过数、历史深度通过数、打分数、因子合格数、目标数和可执行目标数。
- 同时保存股票池剔除原因计数和五个因子族的可用数量。
- 诊断不再内嵌约 600×600 的完整协方差矩阵，只保存矩阵维度、SHA-256 和对角范围。原单期诊断输出超过 6 MB，修复后不会再污染 API 响应或验证证据。
- 有现金仓位时，优化器和最终验收统一按股票已投资权重计算风格暴露，避免总资产口径和股票资产口径不一致。

## 三、真实训练数据诊断结果

本次只使用版本 8 已经接触过的历史训练数据，没有查看或消费新的验证集、留出集。该结果只证明实现链路能够持续运行，不是收益有效性结论。

### 诊断配置

- 区间：2017-02-06 至 2017-04-07
- 动态股票池：沪深 300 + 中证 500 历史成分
- 基准：`H00906` 全收益指数
- 调仓：月末 T 日信号，T+1 起执行
- 初始资金：1,000,000 元
- 最少持仓：20
- 单股上限：5%
- 单行业上限：20%
- 跟踪误差上限：15%
- 权威涨跌停数据覆盖：100%

### 选股和执行

| 信号日 | 信号股票 | 股票池通过 | 因子合格 | 目标持仓 | 成交 | 换手 | 优化状态 |
|---|---:|---:|---:|---:|---:|---:|---|
| 2017-02-28 | 800 | 611 | 322 | 22 | 21 | 46.1377% | optimal |
| 2017-03-31 | 800 | 640 | 330 | 24 | 22 | 35.4154% | optimal |

2017-03-31 最终相对基准风格暴露：

- Size：-0.197884，硬上限 ±0.20；
- Beta：+0.150000，硬上限 ±0.15；
- Liquidity：+0.135558，硬上限 ±0.20。

### 区间指标

| 指标 | 结果 |
|---|---:|
| 交易日 | 43 |
| 调仓期数 | 2 |
| 非空目标期数 | 2 |
| 成交笔数 | 43 |
| 平均换手 | 40.77655% |
| 总费用 | 804.87 元 |
| 平均目标股票仓位 | 99.9999% |
| 区间收益 | +3.501077% |
| H00906 基准收益 | +1.730443% |
| 净超额收益 | +1.770634% |
| 最大回撤 | -1.245922% |
| 跟踪误差 | 4.384648% |

这些收益指标只用于确认净值、费用、成交和基准链路不再退化为现金曲线。区间很短、已被用于诊断且存在实现修复迭代，不能用于选择参数、宣传收益或批准策略。

## 四、正式区间训练走步预检

本轮为正式验证预先固定总区间 `2020-01-02` 至 `2024-12-31`，但只运行由训练段形成的三个带 21 交易日 purge/embargo 的样本外折。验证段和新留出段尚未读取：

- 第一折：`2021-08-03` 至 `2021-12-29`；
- 第二折：`2022-02-07` 至 `2022-07-04`；
- 第三折：`2022-08-03` 至 `2022-12-28`。

固定参数为 Top N 30、单股上限 5%、单行业上限 20%、行业主动偏离 3%、Size/Beta/Liquidity 主动暴露上限 0.20/0.15/0.20、年化波动上限 25%、跟踪误差上限 15%。没有根据训练收益修改参数。

| 训练折 | 调仓期 | 目标持仓范围 | 成交笔数 | 优化状态 | 限价覆盖 | 最低数据覆盖 | 平均换手 | 费用 | 区间收益 |
|---|---:|---:|---:|---|---:|---:|---:|---:|---:|
| 1 | 4 | 25～28 | 89 | 4/4 optimal | 100% | 99.875% | 35.7469% | 1,800.12 元 | -2.4048% |
| 2 | 5 | 25～32 | 118 | 5/5 optimal | 100% | 99.750% | 37.7319% | 2,478.54 元 | -1.4854% |
| 3 | 4 | 25～31 | 113 | 4/4 optimal | 100% | 99.875% | 50.0611% | 2,553.36 元 | -5.3332% |
| 合计 | 13 | 25～32 | 320 | 13/13 optimal | 100% | 99.750% | — | 6,832.02 元 | — |

训练折收益全部为负，但这不是工程故障，也没有用于反向调参。它意味着版本 9 已具备进入正式历史验证的运行条件，却完全不预示验证集或留出集会通过投资有效性门槛。

### 训练中补齐的执行证据

- `600373`：Tushare 限价分区为空，但历史上曾为 `*ST鑫新`。派生规则升级到 `VALIDATED_DERIVED_LIMIT_V2`，按 Tushare 带起止日名称逐日确定 ST 状态，并用 AkShare 名称序列交叉核验；正式区间生成 1,209 条记录。
- `600572`、`601998`：Tushare `namechange` 为成功空响应且限价分区为空。以空响应查询区间、Tushare 当前证券主表和非 ST 当前名称证明正式区间无改名，分别生成 1,212 条记录。
- 多代码派生改为整批验证成功后才原子公开最终 Parquet，避免中途失败留下没有证据报告的半批次文件。

## 五、仍然存在的阻断项

- [x] 补齐训练与正式区间实际成交所需的 `stk_limit` 数据，三个训练折权威覆盖均为 100%。
- [x] 在训练数据范围内完成三个带 purge/embargo 的样本外走步折，并确认每折都有非空目标、成交、换手和费用。
- [x] 补齐正式区间 39 家历史银行专用指标，修复金融因子族覆盖计算。
- [x] 代码、参数、数据清单和运行环境全部冻结后，预注册固定参数与目标指标。
- [x] 一次性执行独立验证集和完全留出集评估，并永久登记留出消费记录。
- [x] 对净超额、主动风险、Rank IC、五档单调性、DSR/PBO、稳定性和成本压力执行门禁；结论为投资证据失败。
- [x] 投资证据和当前 readiness 未通过时不创建两个月模拟前向账户。
- [ ] 继续调查 4,166 条 ROE 跨源差异告警，区分单位、合并口径、报告期和数据错误；当前按字段主源 Tushare 取值的规则虽可审计，但不代表差异已经解决。
- [ ] 改进凸优化器的固定最低佣金近似。当前将最低佣金折算为线性费率，极小预估交易可能造成目标函数尺度失真；真实成交费用计算不受影响，但优化排序仍需专项验证。

## 六、版本 9 当前结论

版本 9 已经达到“可以继续进行正式历史验证”的工程门槛：

- 不再是现金空仓策略；
- 三个训练折 13 期连续产生可执行目标和真实成交；
- 费用、换手、涨跌停顺延与风格约束均实际参与计算；
- 正式区间历史银行没有因缺数或因子族路由错误被整行业剔除；
- 训练成交所需的权威/可审计涨跌停价覆盖为 100%；
- 现金空仓会在训练折立即硬失败；
- 关键选择漏斗可以直接定位下一次失败发生在哪一层。

版本 9 后续已经完成正式历史验证，冻结策略代码为
`08d1370bf3f9124b696d931e2c3eaf92e3dcdd90`，验证结果 SHA-256 为
`76e91b7864e4c7efd983c4eef9a10cd1af2ac21a44d5468e1f419ba0d0396add`。
运行链路门槛通过，但 2024 留出集扣费后超额为 -1.8513%，主动 Alpha、
综合 IC、五档、稳定性和稳健性门禁均未通过，因此没有创建两个月前向账户。
完整证据、失败原因和版本 10 TODO 见
[版本 9 正式历史验证报告](strategy-v9-formal-historical-validation-20260806.md)。
